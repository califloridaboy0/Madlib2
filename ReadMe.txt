We have some default sentence fragments stored in arrays and the user inputs a number to blindly select a sentence fragment.
The user is then prompted to input a verb, noun, animal, etc., which is concocted with a sentence fragment to create a madlib.
